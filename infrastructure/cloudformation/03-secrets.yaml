AWSTemplateFormatVersion: '2010-09-09'
Description: Secrets Manager para Franchise API

Parameters:
  Environment:
    Type: String
    Default: dev
  DBPassword:
    Type: String
    NoEcho: true

Resources:
  DBSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub /accenture/${Environment}/db-credentials
      Description: Credenciales RDS Franchise API
      SecretString: !Sub |
        {
          "username": "admin",
          "password": "${DBPassword}",
          "port": "3306",
          "dbname": "franchise_db"
        }

Outputs:
  DBSecretArn:
    Value: !Ref DBSecret
    Export:
      Name: !Sub franchise-db-secret-arn-${Environment}